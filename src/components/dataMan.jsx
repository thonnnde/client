import {createStore} from 'redux'
const initState = {
    name: 'Jack',
}

const reducer = ( state = initState, action) => {
    switch (action.type) {
        default: 
            return state
        }
}